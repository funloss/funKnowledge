# 2026-04-11

## 创建 book-notes-enricher 技能

- 创建并安装了 `book-notes-enricher` 技能（`~/.workbuddy/skills/book-notes-enricher.skill`）
- 技能核心脚本：`scripts/enrich_notes.py`（纯标准库，无需 pip）
- 功能一：若 YAML `tags` 为空 → 调用 AI（OpenAI 或本地 Ollama）生成 3-5 个中文短标签
- 功能二：若 YAML `cover` 为空 → 爬取 `douban_link` 豆瓣页面的封面图 URL，写入 cover 字段，并将图片下载到 `attachments/` 目录（文件名与 md 同名）
- 读书笔记标准 YAML 字段：`tags`、`douban_link`、`score`、`cover`、`create_time`
- 工作区：`/Users/zhezhang/Documents/Fun Knowledge/读书`（含金融财经、世界历史、文学作品等分类）
